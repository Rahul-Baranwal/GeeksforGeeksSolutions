class Solution {
    void display(Node head) {
        // add code here.
        Node current = head;
        while(current != null){
            System.out.print(current.data+" ");
            current = current.next;
        }
    }
}
