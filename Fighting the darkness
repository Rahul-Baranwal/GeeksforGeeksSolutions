class Solution {
    long maxDays(int arr[]) {
        // code here
        int ans = 0;
        for(int i = 0; i < arr.length; i++){
            if(ans < arr[i]){
                ans = arr[i];
            }
        }
        return ans;
    }
}
